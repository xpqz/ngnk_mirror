name: Build and Test macos

on:
  push:
    branches: [upstream]

jobs:
  buildtestmacos:
    name: Build and Test MacOS
    runs-on: macos-11

    steps:
      - uses: actions/checkout@v2
      - name: make and test
        run: |
          make k-libc
          mv k-libc k
          make t
